let sueldo = prompt(parseInt('ingrese su sueldo'))
let boniMensual = 200
sueldoAnual = sueldo * 12
boniMensualTotal = boniMensual * 12
sueldoAnualTotal = boniMensualTotal + sueldoAnual
console.log(`su sueldo anual es de ${sueldoAnualTotal}`);